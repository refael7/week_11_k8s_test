git checkout -b service
git add .
git commit -m "add service"
git push -u origin service
git checkout main
git pull origin main
git merge service
kubectl apply -f service.yaml

git checkout -b pod
git add .
git commit -m "add pod"
git push -u origin pod
git checkout main
git pull origin main
git merge pod
git checkout -b Dockerfile
kubectl apply -f pod.yaml

git add .
git commit -m "add Dockerfile"
git push -u origin Dockerfile
git checkout main
git pull origin main
git merge Dockerfile
docker build -t refaelsaidov1/week_11_k8s_test:0.0.1 .
docker run -d --name str-app  -p 8080:8080 refaelsaidov1/week_11_k8s_test:0.0.1
http://localhost:8501



